import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    titleText: {
        color: '#ffffff',
        fontSize: 12,
        fontWeight: 'bold',
    }
})

export default styles